const users = {
  //id,
  //email,
  //password(hashed)
};

const urlDatabase = {
  /* shortURL: {
  longURL,
  userID,
  visits,
  uniqueVisits,
  visitsByCreator
  } */
};

module.exports = { users, urlDatabase };